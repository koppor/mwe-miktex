name: MiKTeX (Windows)
on:
  push:
    paths:
      - '.github/workflows/miktex-windows.yml'
      - 'example-expansion.tex'
      - 'example-expansion-T1.tex'
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  miktex:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        # filename: [example-expansion.tex, example-expansion-T1.tex]
        filename: [example-expansion.tex]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Install MiKTeX
        run: |
          choco install miktex --no-progress
          echo "C:\Program Files\MiKTeX\miktex\bin\x64" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8
      - run: initexmf --admin --verbose --set-config-value=[MPM]AutoInstall=1
      - run: miktex --verbose packages update-package-database
      - run: miktex --verbose packages update
      - run: miktex --verbose packages install cm-super
      - run: miktex --verbose fndb refresh
      - run: initexmf --verbose --update-fndb
      - run: type C:\Users\runneradmin\AppData\Local\MiKTeX\miktex\log\initexmf.log
      - run: initexmf --verbose --mklinks --force
      - run: type C:\Users\runneradmin\AppData\Local\MiKTeX\miktex\log\initexmf.log
      - run: updmap
      - run: pdflatex ${{ matrix.filename }}
